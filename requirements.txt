ffmpeg-python
opencv-python
scenedetect[opencv]
numpy
pytest
# Phase 5B OCR (default Tesseract backend). Python wrapper only.
# The native Tesseract executable is a separate prerequisite; see the
# "OCR Setup (Phase 5B)" section in README.md. It is NOT bundled or
# auto-installed by this project.
pytesseract
# Phase 8B GUI (PySide6). Used ONLY by the gui/ package; gui_core and the
# backend never import Qt. Compatible with current Python versions (including
# Python 3.14) while guarding against an unexpected major-version jump.
PySide6>=6.9,<6.12
